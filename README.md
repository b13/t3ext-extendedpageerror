t3ext-extendedpageerror
=======================

Allows to configure better redirects and error messages when a page is not available in the frontend (e.g. due to language restrictions or page access restrictions).